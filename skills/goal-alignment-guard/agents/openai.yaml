interface:
  display_name: "Goal Alignment Guard · 目标对齐"
  short_description: "Align before work, recheck changes, verify delivery"
  default_prompt: "Use $goal-alignment-guard to align the outcome, change boundaries, and acceptance before starting, recheck consequential changes, and verify the result before delivery."
policy:
  allow_implicit_invocation: true
