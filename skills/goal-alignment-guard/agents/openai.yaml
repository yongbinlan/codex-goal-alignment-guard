interface:
  display_name: "Goal Alignment Guard · 目标对齐"
  short_description: "Catch goal drift and clarify only consequential ambiguity"
  default_prompt: "Use $goal-alignment-guard to reconcile my current goal, allowed changes, and preserved constraints before continuing."
policy:
  allow_implicit_invocation: true
